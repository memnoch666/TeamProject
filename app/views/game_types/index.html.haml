%h1 Listing game_types

%table
  %tr
    %th Title
    %th Max score
    %th
    %th
    %th

  - @game_types.each do |game_type|
    %tr
      %td= game_type.title
      %td= game_type.max_score
      %td= link_to 'Show', game_type
      %td= link_to 'Edit', edit_game_type_path(game_type)
      %td= link_to 'Destroy', game_type, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Game type', new_game_type_path
